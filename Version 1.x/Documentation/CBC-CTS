Dooble uses the CBC mode of operation with ciphertext stealing (CTS).

Authenticated and encrypted values are stored in various databases.
The format of the stored objects is H(E(P || Length(P))) || E(P || Length(P)).
